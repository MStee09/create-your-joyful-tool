// Placeholder hooks for data sync - will be implemented with Supabase later

export const useSyncSeasons = () => {
  return { syncing: false, error: null };
};

export const useSyncProducts = () => {
  return { syncing: false, error: null };
};

export const useSyncVendors = () => {
  return { syncing: false, error: null };
};

export const useSyncInventory = () => {
  return { syncing: false, error: null };
};
